<div class="body-main">
	<div class="container">


	</div>
</div>
<div class=body-second>
	<div class="container">
		<h1 class="main-title"> Bienvenue à toi sur l'index de tout les Gossips</h1>
		<p>
  			<a href= "<%= new_gossip_path %>" class="btn btn-success">Créer un Gossip <i class="fas fa-comments"></i></a>
		</p>
	<%Gossip.all.each do |gossip|%>
	<div class="card">
	 <div class="card-body">
	 	<h4>Gossip n°<%= gossip.id%></h4>
	   <p>L'auteur est <strong><%= gossip.author%></strong><br></p>
	   <p><%= gossip.content%></p>
	   <p>
	   	 <a href= "<%= gossip_path(gossip.id)%>" class="btn btn-primary">Lire</a>
	   	 <a href= "<%= edit_gossip_path(gossip.id)%>" class="btn btn-primary"><i class="fas fa-pencil-alt"></i></a>
	   	 <a href= "<%= gossip_path(gossip.id)%>" class="btn btn-danger" data-confirm="Supprimer ?" data-method="Delete"><i class="fas fa-exclamation-triangle"></i></a>
	   </p>
	 </div>
	</div>
	<% end %>
	</div>
</div>


